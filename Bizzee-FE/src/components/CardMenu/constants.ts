import { PopoverOrigin } from "@mui/material";

export const defaultProps = {
  transformOrigin: { horizontal: "right", vertical: "top" } as PopoverOrigin,
  anchorOrigin: { horizontal: "right", vertical: "bottom" } as PopoverOrigin,
};
