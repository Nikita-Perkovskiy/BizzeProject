import { SxProps, Theme } from "@mui/material";

export const styles = {
  title: {
    fontSize: {
      xs: "24px",
    },
    lineHeight: "30.12px",
    fontWeight: 600,
    textTransform: "uppercase",
  } as SxProps<Theme>,
};
